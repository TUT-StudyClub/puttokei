name: backend-cd

# Epic #7（クラウドインフラと CI/CD の構築）で中身を埋める。
# 現時点では invalid workflow file になるのを避けるための no-op 雛形。
# 自動トリガーは無効にし、手動 dispatch のみ受け付ける。

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "backend-cd は Epic #7 で実装予定"
